import { render } from "@testing-library/react"
import DashboardDevice from "../DashboardDevice/DashboardDevice"

describe("DashboardDevice", () => {
    it("should render correctly", () => {
        render(<DashboardDevice />)
    })
})